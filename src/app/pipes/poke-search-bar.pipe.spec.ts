import { PokeSearchBarPipe } from './poke-search-bar.pipe';

describe('PokeSearchBarPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeSearchBarPipe();
    expect(pipe).toBeTruthy();
  });
});
